# Repli SPA. Cloudflare Pages sert d'abord les fichiers statiques réels : une
# règle attrape-tout ne masque donc pas /assets/*. C'est ce qui remplace la
# regex à lookahead négatif de vercel.json, que le format _redirects ne sait
# pas exprimer — et n'a pas besoin d'exprimer ici.
/*    /index.html   200
